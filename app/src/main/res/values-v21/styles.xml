<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/color_primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/color_primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/color_accent</item>


        <item name="android:actionModeStyle">@style/ActionModeStyle</item>
    </style>

    <style name="ActionModeStyle" parent="android:Theme.Material.Light.DarkActionBar">
        <item name="android:height">?android:attr/actionBarSize</item>
        <item name="android:titleTextStyle">@android:style/TextAppearance.Material.Title</item>
    </style>

    <style name="SettingsTheme" parent="android:Theme.Material">
        <item name="android:colorAccent">@color/color_accent</item>
    </style>

    <!--Progress bar style.-->
    <style name="HorizontalProgressBar" parent="android:Widget.Material.ProgressBar.Horizontal">

    </style>
</resources>
